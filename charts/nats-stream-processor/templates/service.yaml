apiVersion: v1
kind: Service
metadata:
  name: {{ include "nats-stream-processor.fullname" . }}
  labels:
    {{- include "nats-stream-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nats-stream-processor.selectorLabels" . | nindent 4 }}
